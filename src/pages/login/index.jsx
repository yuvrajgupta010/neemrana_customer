import Login from "@/components/Login/Login";
import Head from "next/head";

const LoginPage = (props) => {
  return (
    <>
      <Head>
        <title>Neemrana | Login</title>
      </Head>
      <Login />
    </>
  );
};

export default LoginPage;
